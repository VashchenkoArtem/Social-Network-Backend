model User{
    id          Int         @id @default(autoincrement())
    firstname   String
    lastname    String
    nickname    String
    email       String      @unique
    password    String
    avatar      String?
    signature   String?
    birthDate   DateTime?
}